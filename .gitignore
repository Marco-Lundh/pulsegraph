# Environment / secrets (ADR 0009)
.env
*.env
.env.*
# Templates are safe to track; real per-environment files are not.
!.env.example
!.env.prod.example

# Offline deploy lab state (scripts/lab_deploy.sh)
.lab-state

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Build output (uv build / hatchling)
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Playwright MCP session artifacts
.playwright-mcp/
